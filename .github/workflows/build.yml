name: Deploy ChatGPT2Scratch

on:
  push:
    branches: ["main"]

  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: LLK/scratch-gui
          ref: develop
      - run: npm install
      - uses: actions/checkout@v3
        with:
          path: ./chatgpt2scratchaaa
      - run: sh ./chatgpt2scratchaaa/install.sh
      - run: NODE_OPTIONS=--openssl-legacy-provider npm run build
      - name: npm install under chatgpt2scratchaaa
        run: npm install
        working-directory: ./chatgpt2scratchaaa
      - name: Build chatgpt2scratchaaa.mjs for xcratch
        run: NODE_OPTIONS=--openssl-legacy-provider npm run build
        working-directory: ./chatgpt2scratchaaa
      - name: Copy chatgpt2scratchaaa.mjs to build folder
        run: cp ./chatgpt2scratchaaa/dist/chatgpt2scratchaaa.mjs ./build
      - name: Setup Pages
        uses: actions/configure-pages@v3
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: 'build'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
